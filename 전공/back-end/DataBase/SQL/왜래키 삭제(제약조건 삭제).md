
```
show create table {테이블 명}
```

결과 ex:
```
CREATE TABLE `tbl_device_token` (
   `id` binary(16) NOT NULL,
   `token` varchar(500) NOT NULL,
   `school_id` binary(16) NOT NULL,
   `user_id` binary(16) NOT NULL,
   PRIMARY KEY (`id`),
   KEY `FKq4xooc1b0gbnarm4trcbgcbs8` (`school_id`),
   KEY `FKox1jij0bwveb9evvs5ejhrh3a` (`user_id`),
   CONSTRAINT `FKox1jij0bwveb9evvs5ejhrh3a` FOREIGN KEY (`user_id`) REFERENCES `tbl_user` (`id`),
   CONSTRAINT `FKq4xooc1b0gbnarm4trcbgcbs8` FOREIGN KEY (`school_id`) REFERENCES `tbl_school` (`id`)
 ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci

```

제약조건 이름 복붙후 

```
alter table {테이블 명} drop foreign key FKq4xooc1b0gbnarm4trcbgcbs8;(제약조건 명)
```